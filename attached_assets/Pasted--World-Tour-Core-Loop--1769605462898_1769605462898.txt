아, 우리가 기획 단계에서 이야기했던 **World Tour의 게임 규칙(Core Loop)**을 다시 정리해 드릴게요. "그냥 채팅하고 끝"이면 재미가 없으니까, **게임처럼 성취감**을 주기 위해 정했던 규칙들입니다.

지금 Replit이 껍데기(지도)는 만들었으니, 이제 **이 규칙들을 "프롬프트(Prompt)"나 "코드"로 심어주면 됩니다.**

---

### 🌍 World Tour 게임 규칙 (3단계)

#### 1. 입장 규칙: "그냥 수다 떨러 가는 게 아니다"

* **Rule:** 각 도시마다 **해결해야 할 미션(Scenario)**이 하나씩 부여됩니다.
* **Example:**
* 🇫🇷 **파리:** "빵집에서 바게트 2개와 에스프레소를 주문하고, 추천 메뉴를 물어보세요."
* 🇺🇸 **뉴욕:** "지하철역에서 타임스퀘어 가는 길을 행인에게 물어보세요."
* 🇰🇷 **서울:** "편의점에서 컵라면을 사고 조리법을 물어보세요."


* **구현 포인트:** `constants.ts` 파일에 도시별로 `scenario` 텍스트가 이미 들어있거나 추가해야 합니다.

#### 2. 진행 규칙: "호감도(Politeness) 시스템"

* **Rule:** AI NPC는 사용자의 **태도(Manner)**와 **언어 정확성**을 실시간으로 판단합니다.
* **Logic:**
* 처음엔 ❤️ 하트 5개로 시작.
* 무례하거나(욕설), 문맥에 안 맞는 말을 하면 하트가 깎임.
* 하트가 0이 되면 **"대화 실패(Game Over)"** → 추방당함.



#### 3. 승리 조건: "미션 클리어 & 보상"

* **Rule:** 대화가 끝났을 때(사용자가 'Bye' 하거나 목표 달성 시), **AI가 판정**을 내립니다.
* **Checklist:**
1. 목표를 달성했는가? (예: 바게트를 샀는가?)
2. 문법이 적절했는가?


* **Reward:**
* 성공 시: **여권 도장(Stamp)** 획득 + **XP(경험치) 100** 지급.
* 실패 시: XP 10 지급 (노력상), 다시 도전.



---

### 🤖 Replit에게 이렇게 시키면 됩니다 (복사해서 쓰세요)

지금 지도는 구현됐으니, **"채팅이 끝났을 때 승패를 가르는 로직"**을 넣어달라고 하면 됩니다. Replit 채팅창에 아래 내용을 붙여넣으세요.

> **[지시사항]**
> World Tour의 게임 규칙을 코드로 구현해줘.
> 1. **미션 시스템:** `ChatInterface`가 열릴 때, 상단에 **"현재 미션: [도시별 시나리오]"**가 배너로 뜨게 해줘. (예: "뉴욕: 길 물어보기")
> 2. **판정 로직:** 대화가 종료될 때(사용자가 나가기 버튼 클릭 시), 백그라운드에서 Gemini에게 **"사용자가 미션을 성공했는지(True/False)"** 물어보는 API를 호출해줘.
> 3. **보상:** 성공했다면 화면에 **"Mission Complete!"** 모달을 띄우고, `userStats`에 XP 100을 추가해줘. 실패하면 "Try Again"을 띄워줘.
> 
> 

---

### 💡 팁: 데이터 구조 (`constants.ts`)

Replit이 만든 `client/src/lib/constants.ts` 파일에 아마 도시 데이터가 있을 텐데, 거기에 `mission` 필드가 있는지 확인해보세요. 없으면 이렇게 추가해달라고 하세요.

```typescript
// 이렇게 되어 있어야 함
export const DESTINATIONS = [
  {
    id: 'paris',
    name: 'Paris',
    lat: 48.8566,
    lng: 2.3522,
    // 이 부분이 미션입니다!
    scenario: "Order a baguette and a coffee at a local bakery.", 
    characterName: "Pierre",
    greeting: "Bonjour! Bienvenue à ma boulangerie. Que désirez-vous?"
  },
  // ...
];

```

이 규칙대로만 구현되면 **단순 채팅 앱이 아니라 진짜 게임**이 됩니다! Replit에게 바로 시켜보세요! 🚀